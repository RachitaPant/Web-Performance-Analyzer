export default function About(){
    return (<div className="text-white">
     
<p>How It Works:

Users enter a website URL, and the tool analyzes:

How much CPU, RAM, and network bandwidth the site consumes.

Detects JavaScript-heavy bottlenecks, unnecessary API calls, and inefficient rendering.

Gives suggestions to optimize (e.g., reduce reflows, defer scripts, use lazy loading).
</p>
    </div>)
}